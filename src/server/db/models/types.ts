export interface TChirps {
    id: Number,
    userid: Number,
    message: String,
    location: String,
    created_at: Date
}

export interface TUsers {
    id: Number,
    username: String,
    email: String,
    password: String,
    created_at: Date
}

